{
  "apps": [
    {
      "name": "www",
      "script": "server.js",
      "env": { "PORT": "9001" },
      "env_production": { "NODE_ENV": "production" }
    }
  ],
  "deploy": {
    "production": {
      "user": "pm2",
      "host": "regex.plugins.overviewdocs.com",
      "ref": "origin/master",
      "repo": "https://github.com/overview/overview-grep.git",
      "path": "/opt/pm2/overview-grep",
      "post-deploy" : "npm install && pm2 startOrRestart ecosystem.json --env production"
    }
  }
}
